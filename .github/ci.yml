name: ci

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - checkout
      - run: brew update
      - run: brew install shellcheck
      - run: shellcheck ubuntu -e SC2039
      - run: sh ubuntu
